<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tặng điểm cho nhân viên</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
</head>

<body>
    
    <div class="container">
        <h2 class="text-center mt-5">Tặng điểm cho nhân viên</h2>
        <form action="/adminhome/admin/givePoints" method="POST">
            <div class="mb-3">
                <label for="employeeIds">ID Nhân viên (ngăn cách bằng dấu phẩy):</label>
                <input type="text" id="employeeIds" name="employeeIds" class="form-control"
                    placeholder="Nhập ID nhân viên (dấu phẩy ngăn cách)" required>
            </div>

            <div class="mb-3">
                <label for="points">Số điểm</label>
                <input type="number" id="points" name="points" class="form-control"
                    placeholder="Nhập số điểm cần thưởng" required>
            </div>

            <button type="submit" class="btn btn-primary">Tặng điểm</button>

            <% if (message) { %>
                <div class="alert alert-info mt-3">
                    <%= message %>
                </div>
                <% } %>
        </form>
    </div>
</body>

</html>